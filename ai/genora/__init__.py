"""GenOra — the agentic workflow orchestration layer of the GenOra marketplace.

Agents:
* Nova  — buyer agent (implemented)
* Astra — seller agent (implemented)
* Apex  — enterprise agent (planned / NOT implemented; architecture and extension points only)

This package is independent of the web framework and database. Business operations are provided by
the host application as typed tools (see :mod:`genora.core.tools`).
"""

__version__ = "0.1.0"
